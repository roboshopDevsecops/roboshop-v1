---
- name: Install Node.js repository
  ansible.builtin.shell: curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
  args:
    creates: /etc/yum.repos.d/nodesource.repo

- name: Install Node.js
  ansible.builtin.dnf:
    name: nodejs
    state: present

- name: Create appuser
  ansible.builtin.user:
    name: appuser
    system: true
    shell: /bin/false

- name: Download user service artifact
  ansible.builtin.get_url:
    url: "{{ artifact_base_url }}/user.zip"
    dest: /tmp/user.zip

- name: Extract user service
  ansible.builtin.unarchive:
    src: /tmp/user.zip
    dest: /app
    remote_src: true

- name: Install Node.js dependencies
  ansible.builtin.shell: npm install --production
  args:
    chdir: /app

- name: Install user systemd unit
  ansible.builtin.template:
    src: user.service.j2
    dest: /etc/systemd/system/user.service

- name: Start user service
  ansible.builtin.systemd_service:
    name: user
    daemon_reload: true
    enabled: true
    state: started
